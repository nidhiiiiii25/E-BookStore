import { Fragment } from "react";
import Availiblebook from "./Availiblebook";

const book = (props) => {
  return (
    <Fragment>
      <Availiblebook/>
    </Fragment>
  );
};

export default book;
